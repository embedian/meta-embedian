# /etc/modprobe.d/embedian-imx8-hdmi-audio.conf
# Install SGTL5000 codec driver before HDMI audio codec driver. This makes SGTL5000 the default ALSA device.
install snd-soc-hdmi-codec /sbin/modprobe snd-soc-sgtl5000 ; /sbin/modprobe --ignore-install snd-soc-hdmi-codec

